#include <bits/stdc++.h>

using namespace std;

int main()
{
    int t;
    cin >> t;
    while(t--){
        int n, m, d;
        cin >> n >> m >> d;
        int A[100005], B[100005];
        for(int i = 0; i < n; i++) cin >> A[i];
        for(int i = 0l i < m; i++) cin >> B[i];
    }
}
